@model System.Data.DataTable
@using System.Data;

@{

    var total = ViewBag.UltimoRegistro;
    var ceros = "0000";
    var completo = ceros + total;
    //Char delimiter = '0';
    //String[] substrings = total.Split(delimiter);
    //foreach (var substring in substrings)
    //{
    //    Convert.ToInt64(substrings);
    //var uno = Convert.ToInt64(substrings) + 1;
    //}




    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Masteradmin.cshtml";
}
<div class="row">
    <div class="col-md-12">
        <div class="well">
            <h2></h2>

            @using (Html.BeginForm("Upload", "Reconocimientos", null, FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary()

                <div class="form-group">
                    <input type="file" id="dataFile" name="upload" />
                </div>

                <div class="form-group">
                    <input type="submit" value="Cargar" class="btn btn-default" />
                </div>

                @*@Html.Partial("_Datos")*@


            }

            @using (Html.BeginForm("CargaMasiva", "Reconocimientos", null, FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary()
                @Html.Partial("_Datos")

                <input type="submit" value="enviar" />
            }
        </div>
    </div>
</div>
